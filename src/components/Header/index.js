import { HeaderName } from "./styledHeader";

const Header = () => {
  return (
    <header>
      <nav>
        <HeaderName className="header-name">Products</HeaderName>
      </nav>
    </header>
  );
};
export default Header;
